const HTTP_EXCEPTIONS = require('./errorExceptions/HttpException.contants');
const ERROR_DTO_PATTERNS = require('./errorExceptions/errorDTO.constants');
const REGULAR_EXPRESSIONS = require('./regex.constants');

const CONSTANTS = {
  REGULAR_EXPRESSIONS,
  ERROR_DTO_PATTERNS,
  HTTP_EXCEPTIONS,
};

module.exports = CONSTANTS;
